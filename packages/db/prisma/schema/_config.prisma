generator client {
  provider = "prisma-client-js"
  output   = "../../generated/client"
}

generator zod {
  provider   = "prisma-zod-generator"
  output     = "../../generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
